get:
  summary: 都道府県一覧を取得
  operationId: get-prefectures
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../schemas/Prefecture.yml"
